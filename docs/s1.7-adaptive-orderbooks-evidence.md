# S1.7 adaptive order books — local evidence

Date: 2026-08-30; closure evidence updated 2026-09-10. Branch:
`codex/adaptive-orderbooks`. Baseline:
`b7b0e7060f4c00d7fcb072d78f02dfb59be2ee9e`. Status: implementation,
P0/P1 independent audit, build/tests/CI, package and recovery verification pass
for current code head `9af7e0b`. Fresh visual acceptance and a quiet-host
15-minute guard passed on predecessor package `6474cdf...`; the current package
adds only the independently accepted settings-overlay accessibility correction.
Its historical exact-hash GUI/soak attempt was `BLOCKED_ENVIRONMENT`; the
two-hour gate is `NOT RUN`. On 2026-09-10 the Founder waived the remaining
current-package GUI and 5/15/30/120-minute checks for this prerelease only.
Status is `ACCEPTED_WITH_RISK`, not runtime PASS; see
[release acceptance](s1.7-release.md). Runtime guards stay enabled.

## User result

- BTC/USDT opens by default in the app and deterministic selection fixtures.
- MEXC, Gate and Bybit USDT perpetual cards can all create public book clients.
  MEXC private perpetual trading remains `Blocked`.
- Each card fills its available height and owns automatic/manual depth, a
  fine-grained vertical trackpad step, automatic/manual volume reference and
  four editable HEX bar colors.
- Owner defaults are ask yellow, largest visible ask red, bid blue and largest
  visible bid green. Ask and bid maxima are calculated independently, so both
  visible side maxima reach 100% width.
- A twelve-pixel safety gap on both sides of the spread row prevents the nearest
  ask/bid text and bars from being clipped by the central control strip.
- Settings persist locally in a bounded versioned file under the standard
  per-user application-data directory. Writes are latest-wins, atomic,
  flushed off the UI thread and awaited by the cancellable normal shutdown
  path. Invalid/corrupt/oversized state fails closed to defaults.

## Public data boundary

- Bybit publishes up to 200 captured levels; Gate up to 50; MEXC Spot up to 200.
- MEXC perpetual uses the official credential-free contract catalog and bounded
  public REST depth polling. It validates success/code, version, two non-empty
  uncrossed sides, prices, contract counts, duplicate levels, price tick and
  contract multiplier. Official source checked 2026-08-30:
  <https://mexcdevelop.github.io/apidocs/contract_v1_en/>.
- Startup catalogs use a dedicated 15-second no-redirect/no-cookie HTTP client;
  live MEXC REST market data retains a five-second client. Responses use the
  shared bounded reader.
- One bounded credential-free BTC smoke through the actual clients passed:
  MEXC `BTC_USDT` 200 bids/200 asks, Gate 50/50 and Bybit `BTCUSDT` 200/200.

## Verification

- New deterministic source covers adaptive/manual depth, bounded trackpad
  adjustment, independent ask/bid volume references, palette validation, MEXC
  contract metadata/depth normalization, Gate per-entry rejection, BTC startup
  and three-venue perpetual layout. Closure tests additionally cover catalog
  refresh/rollback/reconciliation and settings persistence/close flush.
- Targeted Release test-assembly compilation: `PASS`, 0 warnings/errors.
- Superseded initial VSTest attempt: sandbox IPC blocked. The authorized final
  local VSTest ran outside that sandbox boundary. Its first full run found one
  stale replay expectation (366/367) after atomic client staging; production
  behavior was bounded and independently accepted. After strengthening the
  expected transient count to exact four, the final full run was `367/367
  PASS`, 0 failed, 0 skipped.
- Final Release solution build: `PASS`, 0 warnings/errors. The final headless
  replay processed 1,000,000 cycles and ended with created clients equal to
  disposed clients; all memory budgets passed.
- Final self-contained `osx-arm64` package replaced only the existing ignored
  `artifacts/TRDNG.app`; strict deep ad-hoc codesign: `PASS`.
- Current publish/app `Trdng.Desktop.dll` SHA-256:
  `304db925690b671645401a0d01b92416212fb6ae323ded869935b825b33ab062`.
- Current signed app executable SHA-256:
  `07a2dd98b5353ed7159db15981765575155d0f68248fe33c0bbecebbd46b204f`.
- Historical predecessor package `1d93a3...` guarded five-minute run:
  `PASS_DURATION`; peak physical
  footprint 191,515,776 bytes, swap-out delta 0, cleanup PASS.
- The next `1d93a3...` 15-minute attempt stopped at 6m25s on immutable
  `SYSTEM_SWAP_GROWTH`; app peak/final physical footprint was
  199,281,728/190,204,992 bytes and no owned PID remained. Classification:
  `BLOCKED_ENVIRONMENT`; 30-minute and two-hour gates were not started.
- Predecessor package `6474cdf...` launched with BTC/USDT as the default
  selection.
  Fresh visual acceptance proved MEXC/Gate/Bybit `LIVE`, populated books at the
  default and large/full-screen window, all three independent settings buttons,
  and fully visible largest ask/bid rows. The original flyout did not open in
  the exact app; it was replaced with one bounded central overlay that was
  visually verified for MEXC, Gate and Bybit. The Founder-reported sales-side
  error was reproduced as the nearest/largest ask being clipped under the
  spread strip. The symmetric twelve-pixel reservation corrected it: the
  largest visible ask is fully red and reaches full width on all three venues,
  while other asks remain yellow and scale against that visible maximum.
  Computer Use could not prove the exact trackpad wheel event; deterministic
  fine-grained/bounded adjustment tests remain PASS and GUI gesture acceptance
  is explicitly `NOT PROVEN`.
- A predecessor `6474cdf...` visual-stress 15-minute run that included
  full-screen transitions and
  Computer Use completed the duration with zero swap growth, but its physical
  footprint rose to 819,040,448 bytes. It is retained as
  `VISUAL_STRESS COMPLETED / MEMORY NOT ACCEPTED`, not release evidence.
- A separate predecessor-package exact-hash 15-minute run with no Computer Use
  passed: 88 samples,
  peak/final RSS 245,776,384/113,639,424 bytes, peak/final physical footprint
  230,296,768/211,405,952 bytes, swap delta 0 and no remaining owned process.
- Closure checks: `git diff --check` PASS; repository Markdown links PASS;
  targeted credential/private-key marker scan zero; excluded local-only
  project-term scan zero. Code and prior documentation through `a86ece1` are
  published to PR #10. The waiver documentation still awaits commit and CI;
  green final checks remain required before merge.

## Debt and boundaries

- `CLOSED`: implementation commit
  `fb252bfa0afda8f57d51202d74baeb29e8954d79` and correction head
  `83e92ba4aca685abc21888cb24317a2c611eb39d` are published to PR #10. Corrected
  CI run `33392591048`: Release build PASS, 367/367 tests PASS and bounded
  one-million-cycle replay PASS. The preceding run `33392273068` failed before
  compilation on CI tmpfs capacity; the audited cache-location fix did not
  weaken memory/no-swap limits.
- `CLOSED` on predecessor package `6474cdf...`: populated-book, spread-overlap,
  all-three-settings and large-window visual acceptance. Trackpad wheel
  synthesis is retained
  as a non-release GUI evidence gap; its deterministic policy tests pass.
- `CLOSED` on predecessor package `6474cdf...`: quiet-host exact-hash 15-minute
  guarded gate.
- Historical `BLOCKED_ENVIRONMENT`, now waived for this prerelease:
  quiet-host 30-minute gate. Three exact-package attempts
  stopped fail-closed on host-wide swap growth while the app stayed far below
  its 512 MiB healthy target: 2026-08-31 after 5m10s at 207,949,120 bytes peak
  footprint; 2026-09-10 after approximately 20s at 163,417,024 bytes; and one
  controlled retry after 10s at 152,030,208 bytes. Between the two 2026-09-10
  launches, an operator-observed no-app baseline sampled the same swap counter
  `39228865` twelve times from `10:30:09Z` through `10:31:59Z`; this observation
  is not a watchdog artifact. The watchdog thresholds are unchanged and no
  further retry or system-memory workaround was attempted.
- `NOT RUN`, now waived for this prerelease: quiet-host 120-minute release gate
  because its 30-minute prerequisite did not pass.
- `CLOSED`: the bounded settings overlay now closes through its button, Escape
  or backdrop; Tab cycles inside it and prior focus is restored. Independent
  audit found no P0/P1/P2. Current-package GUI execution was prevented by the
  same host-wide swap gate before visual input.
- `CLOSED FOR MVP`: MEXC perpetual polling measurement is recorded in
  [`mexc-polling-evidence.md`](mexc-polling-evidence.md); it remains explicitly
  unsuitable for real-order execution without a future transport sprint.
- `CLOSED`: per-book settings have bounded local persistence and graceful-close
  flush. Forced process termination still cannot guarantee an application write.
- `NOT RUN` in this closure: private/authenticated calls, `/order/test`,
  production orders, money actions and notarization. Tag/publication state is
  recorded in [release acceptance](s1.7-release.md). Terminal-only restore PASS for
  current code head `9af7e0beb64a8bee7d311048ecde21f3519b01e5`.
- Accessibility correction commit `9af7e0beb64a8bee7d311048ecde21f3519b01e5`
  is published to PR #10. CI run `34470736716`: Release build PASS, 367/367 tests
  PASS and the one-million-cycle replay PASS.
- Current exact-package guarded attempt stopped after 10 seconds on
  `SYSTEM_SWAP_GROWTH` at 158,485,440 bytes footprint; cleanup PASS. It is not a
  GUI or soak PASS claim.

No API key, secret, signature, signed URL or private response payload was read,
logged or added to this evidence.
